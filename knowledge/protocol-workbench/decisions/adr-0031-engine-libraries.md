---
id: adr-0031
date: 2026-06-26
grill-ref: deep-dive — engine libraries (build-tools, generator, mock-runner)
status: accepted
changes: [build-tools, api-service-generator, mock-runner-lib, automation-libraries, spec-logic, onix-adapter, db-service, spec-to-runtime]
---

# Seed engine-libraries sub-book

## Decision
Deep-dived the three engines (now submodules):
- build-tools: commands (parse/validate/gen-rag-table/push-to-db/changelogs/markdowns/knowledge-book/polish), build.yaml Zod schema, semantic validations, raw_table.json shape, ingest collections (build_*).
- api-service-generator: ONIX pipeline (schemas, clone plugins, L1 go validations, adapter+routers gen, docker), templated-vs-static, plugin compilation, base image.
- mock-runner-lib: MockRunner API, MockPlaygroundConfig grammar, the three base64 JS functions (generate/validate/meetsRequirements + getSave) with timeouts, VM/worker sandbox + fetch allowlist + CodeValidator, saveData (APPEND#/EVAL#) session fullness, version-aware context, helpers.

## Assumptions & perception
- Source: Explore over automation-utils/build-tools, automation-api-service-generator, automation-mock-runner-lib (file:line). High confidence.
- Confirms api-service/mock-service does NOT import mock-runner (reinforces adr-0025: embedded mock is separate/static).

## KB effect
- new frames build-tools, api-service-generator, mock-runner-lib under [[automation-libraries]].
- spec-logic (canonical fn signatures + sandbox), onix-adapter (generated by generator), db-service (build_* collection names), spec-to-runtime (engine links).
